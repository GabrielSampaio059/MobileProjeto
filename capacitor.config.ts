import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'livraria_atena',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
